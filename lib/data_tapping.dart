import 'package:flutter/material.dart';

class data_tapping extends StatefulWidget {
  const data_tapping({Key key}) : super(key: key);

  @override
  State<data_tapping> createState() => _data_tappingState();
}

class _data_tappingState extends State<data_tapping> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Data Tapping'),
    );
  }
}
